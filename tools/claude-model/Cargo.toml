[package]
name = "ushas-claude-model"
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
publish = false
description = "Shared procedural Claude toy assets for Ushas diagnostic tools"

[dependencies]
bevy = { version = "=0.19.0", default-features = false, features = [
    "bevy_asset", "bevy_color", "bevy_pbr", "bevy_render",
] }
